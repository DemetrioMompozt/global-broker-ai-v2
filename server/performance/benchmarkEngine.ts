export function getBenchmarkPlaceholder() {
  return {
    enabled: false,
    reason: 'Benchmarks quedan fuera del flujo operativo v2 inicial para evitar mezclar daily benchmark con precio vivo.',
  }
}
